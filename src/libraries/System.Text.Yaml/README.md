# System.Text.Yaml

This project defines the `System.Text.Yaml` library. It provides basic types for parsing and serializing YAML text. The goal of this new package is to follow the same design principles as `System.Text.Json` and eventually enable a `LoadYamlConfiguration` feature for ASP.NET.

This initial commit contains a very small subset of functionality intended as a starting point for further development.
